# /styles

Tailwind config lives at repo root; `tokens.css` holds the fixed design
system as CSS variables (primary #2F6B3B, secondary #8AAE5D, accent
#D6C28F, background #FAFAF7, text #1A1A1A, 12px radius, 8px grid, Inter).

Populated in **Phase 1**.
