# /prisma

`schema.prisma`, generated `migrations/`, and `seed.ts` (loads the fixed
12-product catalogue).

Populated in **Phase 1**. See `docs/03-database-er-diagram.md`,
`docs/04-prisma-schema.md`, `docs/05-sql-migration.md`,
`docs/06-seed-script.md`.
