# /app

Next.js 14 App Router routes. Route groups: `(auth)`, `(shop)`, `account`,
`admin`, `blog`, plus `api/` route handlers.

Populated in **Phase 4** (pages) and **Phase 3** (api routes). See
`docs/01-repository-tree.md` for the exact file list.
