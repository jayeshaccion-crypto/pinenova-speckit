# /hooks

Custom React hooks wrapping TanStack Query + Zustand state: `use-cart`,
`use-auth`, `use-products`, etc.

Populated in **Phase 3** (auth) / **Phase 4** (products) / **Phase 5**
(cart). Defined in `docs/12-state-management.md`.
