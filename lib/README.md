# /lib

Core server-side libraries: Prisma client singleton (`db.ts`), auth helpers,
Stripe client, S3 client, Pino logger, and the repository-pattern data
access layer (`repositories/`).

Populated in **Phase 1** (db, s3, logger, repositories) and **Phase 3**
(auth) / **Phase 5** (stripe).
